---
- job:
    name: elastic+logstash-docker+7.1+snapshot
    display-name: 'elastic / logstash-docker # 7.1 - snapshot'
    description: Periodic testing of snapshot builds for the Logstash Docker 7.1 branch.
    parameters:
    - string:
        name: branch_specifier
        default: refs/heads/7.1
        description: the Git branch specifier to build (&lt;branchName&gt;, &lt;tagName&gt;,
          &lt;commitId&gt;, etc.)
