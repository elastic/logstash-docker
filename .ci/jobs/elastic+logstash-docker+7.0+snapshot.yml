---
- job:
    name: elastic+logstash-docker+7.0+snapshot
    display-name: 'elastic / logstash-docker # 7.0 - snapshot'
    description: Periodic testing of snapshot builds for the Logstash Docker 7.0 branch.
    parameters:
    - string:
        name: branch_specifier
        default: refs/heads/7.0
        description: the Git branch specifier to build (&lt;branchName&gt;, &lt;tagName&gt;,
          &lt;commitId&gt;, etc.)
